# Local development ONLY (includes uvicorn for local testing)
# For production on Vercel, see requirements.txt which is minimal
-r requirements.txt
uvicorn[standard]==0.32.1
pytest==7.4.4
httpx==0.27.0
